{
  "name": "profile-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd my-profile/backend && npm install && npm run build && cd ../frontend && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}